{
  "name": "flexdata",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "serve": "php -S localhost:8000 -t public",
    "watch": "node build.js --watch"
  },
  "dependencies": {
    "esbuild": "^0.21.5"
  }
}